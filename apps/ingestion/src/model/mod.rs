// Re-export common domain models
pub use models::package::Package as PackageMetadata;
pub use models::package::PackageVersion;

// Define specific normalized structures if different from domain
// For now, mapping directly to domain models is a good start.
