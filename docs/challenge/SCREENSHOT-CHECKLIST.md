# Screenshot Checklist

- Graph with selected package
- Selected package evidence attribution and status
- Scenario findings
- Version exposure counts
- Human priority and exclusion state
- Critical paths with priority and hop count
- Structured migration plan
- Adaptive WebMCP tool surface, where the browser exposes it

Capture desktop at 1440x900 and 1280x720, plus the existing mobile viewport. Check that the primary graph controls remain usable and keyboard focus is visible.
